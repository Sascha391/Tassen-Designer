rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Bestellungen: jeder Kunde darf eine anlegen, nur der eingeloggte Admin darf lesen/ändern
    match /tassen_bestellungen/{doc} {
      allow create: if true;
      allow read, update, delete: if request.auth != null;
    }

    // Produkte: jeder darf lesen (für die Kundenseite), nur der Admin darf ändern
    match /tassen_produkte/{doc} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    // Design-Hintergründe, Farben, Rahmenfarben, Vorlagen:
    // jeder darf lesen (Kundenseite), nur der eingeloggte Admin darf ändern
    match /tassen_hintergruende/{doc} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    match /tassen_hgfarben/{doc} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    match /tassen_rahmenfarben/{doc} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    match /tassen_vorlagen/{doc} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
